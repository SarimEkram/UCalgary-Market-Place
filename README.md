!!Set Up Instructions 

1. docker compose up --build, if having trouble do sudo docker compose up --build 
1A. Adding the -d option will put all of the messages in a log file, keeping the terminal clean. 
!! Password Strategy 
2. Change port numbers, 8080, and 3000 in compose file. 